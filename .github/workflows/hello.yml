name: Hello World CI

on:
  push:
    branches: [ "main" ]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Dizer Olá
        run: echo "Hello World do GitHub Actions 🎉"
